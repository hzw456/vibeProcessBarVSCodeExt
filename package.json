{
  "name": "vibe-process-bar-ext",
  "displayName": "Vibe Process Bar Extension",
  "description": "Auto-detect AI coding activity and notify Vibe Process Bar",
  "version": "1.1.0",
  "publisher": "vibeprocessbar",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-repo/vibe-process-bar-ext.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vibeProcessBar.start",
        "title": "Vibe Process Bar: Start"
      },
      {
        "command": "vibeProcessBar.status",
        "title": "Vibe Process Bar: Check Status"
      }
    ],
    "configuration": {
      "title": "Vibe Process Bar",
      "properties": {
        "vibeProcessBar.endpoint": {
          "type": "string",
          "default": "http://localhost:31415",
          "description": "Vibe Process Bar API endpoint"
        },
        "vibeProcessBar.charThreshold": {
          "type": "number",
          "default": 200,
          "description": "Character change threshold to detect AI activity"
        },
        "vibeProcessBar.idleTimeout": {
          "type": "number",
          "default": 5000,
          "description": "Idle timeout in ms before completing task"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^5.0.4"
  }
}